language: node_js
node_js:
  - "lts/carbon"
  - "lts/dubnium"
  - "11"
cache:
  yarn: true
  directories:
    - node_modules
install:
  - yarn install
script:
  - yarn build
after_script:
  - yarn test:coverage && nyc report --reporter=text-lcov | coveralls
notifications:
  email:
    on_success: change
    on_failure: always
