{
  "name": "dashp",
  "version": "0.9.0",
  "description": "Utilities for monadic promises.",
  "main": "_dist/dashp.js",
  "browser": "_dist/dashp.js",
  "module": "lib/index.js",
  "engines": {
    "node": ">=4"
  },
  "config": {
    "srcDir": "lib",
    "testDir": "test",
    "distDir": "_dist"
  },
  "scripts": {
    "commitmsg": "! git rev-parse --abbrev-ref HEAD | grep -q master || validate-commit-msg",
    "precommit": "! git rev-parse --abbrev-ref HEAD | grep -q master || (lint-staged && npm run test)",
    "prepublishOnly": "npm run build",
    "publish": "git push --follow-tags origin master",
    "watch": "npm run test -- -w",
    "test": "cross-env NODE_ENV=test mocha -c -R progress --require babel-core/register -r test/helpers.js $npm_package_config_testDir/**/*.spec.js",
    "coverage": "nyc npm test",
    "lint": "eslint --cache $npm_package_config_testDir $npm_package_config_srcDir",
    "fix": "npm run lint -- --fix",
    "compile": "npm run clean -s && rollup -c",
    "clean": "rimraf $npm_package_config_distDir",
    "build": "npm run lint -s && npm run test -s && npm run compile -s",
    "release": "npm run build -s && standard-version -i Changelog.md -m \"chore(release): Bumped to version %s.\" --sign",
    "release-candidate": "npm run release -- --prerelease \"rc\""
  },
  "repository": "https://github.com/critocrito/dashp.git",
  "keywords": [
    "async",
    "promise",
    "promises",
    "promises-aplus",
    "functional",
    "static-land",
    "monad",
    "monads",
    "monadic"
  ],
  "author": "Christo <christo@cryptodrunks.net>",
  "license": "GPL-3.0",
  "bugs": "https://github.com/critocrito/dashp/issues",
  "homepage": "https://github.com/critocrito/dashp#Readme.md",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-eslint": "^8.2.1",
    "babel-plugin-external-helpers": "^6.22.0",
    "babel-plugin-istanbul": "^4.1.5",
    "babel-plugin-syntax-async-functions": "^6.13.0",
    "babel-plugin-transform-regenerator": "^6.26.0",
    "babel-polyfill": "^6.26.0",
    "babel-preset-es2015": "^6.24.1",
    "babel-register": "^6.26.0",
    "bluebird": "^3.5.1",
    "chai": "^4.1.2",
    "chai-as-promised": "^7.1.1",
    "cross-env": "^5.1.3",
    "eslint": "^4.18.0",
    "eslint-config-airbnb-base": "^12.1.0",
    "eslint-config-prettier": "^2.9.0",
    "eslint-plugin-import": "^2.8.0",
    "eslint-plugin-lodash-fp": "^2.1.3",
    "eslint-plugin-prettier": "^2.6.0",
    "eslint-plugin-promise": "^3.6.0",
    "husky": "^0.14.3",
    "in-range": "^1.0.0",
    "jsverify": "critocrito/jsverify#throws-regex",
    "lint-staged": "^6.1.1",
    "lodash": "^4.17.5",
    "mocha": "^5.0.1",
    "nyc": "^11.5.0",
    "prettier": "^1.10.2",
    "rimraf": "^2.6.2",
    "rollup": "^0.56.1",
    "rollup-plugin-babel": "^3.0.3",
    "rollup-plugin-uglify": "^3.0.0",
    "sinon": "^4.3.0",
    "standard-version": "^4.3.0",
    "time-span": "^2.0.0",
    "validate-commit-msg": "^2.14.0"
  }
}
